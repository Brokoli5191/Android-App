# Add project specific ProGuard rules here.
# You can control the set of applied configuration files using the proguardFiles setting in build.gradle.

# Keep Room schema (optional)
#-keep class androidx.room.** { *; }
